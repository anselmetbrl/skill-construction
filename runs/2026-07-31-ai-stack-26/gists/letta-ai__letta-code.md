# letta-ai/letta-code
what-it-is   a stateful agent harness for agents "more like people than tools" — with memory, identity, experience over time
why-it-is    tool-like agents don't persist experience; this lets them learn and self-modify (memory, skills, prompts, even the harness)
how-it-is
  technology   TypeScript/Node CLI; MemFS where "All context (including memory blocks) is tracked via git"
  technicality `npm install -g @letta-ai/letta-code`; desktop app + browser/mobile + Slack/Telegram/Discord channels
leaves       skill-learning · `/sleeptime` periodic dreaming · `/palace` memory view · subagents & multi-agent · hooks · crons · self-configuring agents
flags        none — feature-rich, but the features are enumerable and concrete
trust        whole
#graph       active successor of letta (08) ✓ IN-COLLECTION · agent-harness-with-skills shape parallels skill-construction (17), the very harness this run executes in (thematic)

## position (multi-pass)
C3 agent-harness · HIGH. the live node; the collection's closest mirror of this run's OWN harness (skills+subagents+hooks).

## #graph (resolved)
alternative-to → letta (this repo is superseded BY letta-code; self-stated succession)
