# letta-ai/letta
what-it-is   a platform/server for stateful agents with advanced long-term memory (formerly MemGPT)
why-it-is    LLM agents are stateless; this gives them memory that "can learn and self-improve over time"
how-it-is
  technology   the legacy API server; a TypeScript Agent SDK; fully model-agnostic
  technicality npm CLI (via letta-code); agents run on Constellation (agent cloud), locally, or a self-hosted App Server
leaves       memory that self-improves · model-agnostic (recommends latest Anthropic/OpenAI/zAI) · a public model leaderboard
flags        maturity (self-stated, honest): "This repository contains the legacy Letta server... Active development has moved to the Letta Agent repo [letta-code]"
trust        whole — the legacy status is disclosed plainly, not hidden
#graph       "development has moved to letta-code" → superseded-by letta-ai/letta-code (09) ✓ IN-COLLECTION · lineage "formerly MemGPT"
