# boundaryml/baml
what-it-is   a purpose-built programming language for LLM agents ("Basically A Made-up Language")
why-it-is    general-purpose code lets agents make mistakes; a typed, agent-first language "built so agents make fewer mistakes"
how-it-is
  technology   a compiler (Rust, inferred) for a TypeScript-like syntax with a Rust-like type system; types persist at runtime; errors typed + statically analyzed
  technicality install via brew/CLI; callable from TS/Py/Go/C#/Java; "run standalone or adopt incrementally"
leaves       "no any nor casting" · built-in tests/eval framework · a stdlib for agents · filesystem-as-namespaces
flags        none — the Rust/Go comparisons are stylistic analogies, not competitor claims
trust        whole
#graph       LLM/agent-programming cluster: dspy (21), symbolicai (18) — share-tag, not an evidenced edge

## position (multi-pass)
C1 llm-programming · lang/agent · HIGH. the language pole; neighbors dspy (framework) and symbolicai.
