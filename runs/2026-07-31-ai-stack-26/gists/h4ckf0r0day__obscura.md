# h4ckf0r0day/obscura
what-it-is   a Rust headless-browser engine for web scraping and AI-agent automation
why-it-is    headless Chrome is heavy and detectable at automation scale; this targets low-footprint, stealthy automation
how-it-is
  technology   Rust; runs real JavaScript via V8; speaks the Chrome DevTools Protocol
  technicality "drop-in replacement for headless Chrome with Puppeteer and Playwright"; single binary
leaves       built-in anti-detect · self-reported low memory/startup vs Chrome · "open-source engine stays Apache-2.0... No feature gating, ever"
flags        marketing-hype — self-benchmark table with bolded wins; "Obscura Cloud" hosted-version waitlist; sponsor "7M+ residential IPs"; "10,000 stars and what's next" banner
trust        slightly eroded — the capability claims are plausible but SELF-measured and wrapped in a sales register; user weighs
#graph       "drop-in replacement for headless Chrome / Puppeteer / Playwright" → alternative-to (NOT in collection) · AI-agent tooling theme: stereOS (11), screenpipe (25)
