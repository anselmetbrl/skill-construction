# anselmetbrl/skill-construction
what-it-is   the user's own meta-project — the repo in which THIS skill (holistic-meta-index) is being co-constructed
why-it-is    build a reusable natural-language pipeline that turns a curated repo list into a navigable bird's-eye meta-index
how-it-is
  technology   Markdown only — a declarative SDD spec plus stance files; no program code
  technicality lives as `.claude/skills/holistic-meta-index/` (SKILL.md + references); the design record is ledger/spec/vocabulary
leaves       CLAUDE.md (the working stance) · spec.md (the pipeline) · ledger.md (the deconstruction log) · self-referential to this very run
flags        none — GitHub metadata is `#void` (0 stars, no description): a fresh working repo, a finding not a fault
trust        whole
#graph       SELF-REFERENTIAL — this repo produced the pipeline running now · agent-skill theme: letta-code (09) — thematic only, no evidenced edge

## position (multi-pass)
C3/meta · agent · MED. the self-referential root — the repo that authored this very pipeline.
