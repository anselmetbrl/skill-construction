# dynobo/normcap
what-it-is   an OCR-powered screen-capture tool that grabs the TEXT on screen — "capture information instead of images"
why-it-is    often you want the text shown on screen, not a screenshot image of it
how-it-is
  technology   a desktop OCR tool (Python/Qt, inferred)
  technicality cross-platform prebuilt releases — Windows msi/zip, Linux Flathub flatpak, macOS
leaves       "capture information instead of images" · Linux/macOS/Windows · README candidly lists its own alternatives
flags        none
trust        whole
#graph       screen-capture theme with screenpipe (25) but a DIFFERENT function — point-in-time OCR text-grab vs continuous recording → share-tag (screen_capture), not an evidenced edge

## position (multi-pass)
C4 capture · viz · MED-LO. the grab-text capture node; shares the screen_capture tag with screenpipe but simpler and freely-licensed.
