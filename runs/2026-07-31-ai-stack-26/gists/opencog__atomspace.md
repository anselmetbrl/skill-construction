# opencog/atomspace
what-it-is   OpenCog's in-RAM (hyper-)graph knowledge-representation database with a query engine and graph-rewriting system
why-it-is    a knowledge-representation substrate for AGI — a generalized hypergraph (metagraph), more flexible than a plain graph store
how-it-is
  technology   C++ core (scheme/python bindings); an in-RAM generalized hypergraph ("metagraph"); query engine + graph re-writing
  technicality mature; a large ecosystem of modules built atop it — atomspace-rocks (disk), atomspace-cog (network), cogserver, matrix (embeddings)
leaves       "a metagraph store is literally just-plain better than a graph store" (links a PDF as evidence) · "a platform for building Artificial General Intelligence (AGI) systems" · "several dozen modules built on top"
flags        none — the "better than a graph store" claim offers a PDF, so it reads as an argued position, not a bare superlative
trust        whole
#graph       OpenCog Classic KR store; hyperon (06) self-describes as "successor to the OpenCog Classic Atomese language" → succeeded-by hyperon ✓ IN-COLLECTION · graph-data theme: typedb (14)

## position (multi-pass)
C2∩C7 · db/research · HIGH. the mature KR substrate; hyperon's predecessor, typedb's AGI-oriented cousin.

## #graph (resolved)
succeeded-by ← hyperon (OpenCog Hyperon positions as successor to OpenCog-Classic KR)
