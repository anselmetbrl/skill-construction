# gist — anselmetbrl/skill-construction · seed 17

    source · dossiers/anselmetbrl__skill-construction.md, frozen 2026-07-31
      #void — provenance line, not a claim
    all spans below are pinned to commit 6c1763a3, the default-branch head that
      pre-dates every commit this run has made
      #void — provenance line, not a claim

## what-it-is

    a spec for a pipeline that turns a curated repo list into a navigable meta-index
      ← "> **what this is.** a declarative v1 spec for a pipeline that turns a
         hand-curated list of github repos into a navigable meta-index giving BIRD'S-EYE
         orientation. one file. syntax-agnostic."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    and, beside it, a standing stance governing how the model works with one person
      ← "> **what this is.** a standing stance for how to work with this user, auto-loaded
         each session. written TO you, the model. it governs HOW you act — not WHAT is
         built."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/CLAUDE.md)
    the two are deliberately separated — WHAT in one file, HOW in the other
      ← "the WHAT; `CLAUDE.md` holds the HOW-to-work-with-the-user."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)

## why-it-is

    the need is named as a scarcity, and located in the collection rather than the repo
      ← "why    the bottleneck is attention and access, not storage         (R9 R10) /
         reading repo-by-repo yields only depth, never position — position exists only in
         the collection"
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    with an explicit limit on what the artifact is allowed to replace
      ← "## 0 · root — orientation, not substitution / > the purpose every node below
         serves. the index gives POSITION and ACCESS. the user still reads the repos
         themselves."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)

## how-it-is · technology (internal)

    every claim node folds into the conditions under which it becomes wrong
      ← "each node folds into a **falsification**: / - `moderation(~invalidation)` — when
         this decision becomes mistaken. / - `modification(~verification)` — how to
         re-read, what nuance recontextualises it then."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    the document's shape is a graph, and the outline is only a reading-view of it
      ← "nodes **cross-link by name** (`links`), not by id. hierarchy is a reading-view;
         the real shape is a graph."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    the stance is written as leanings against named vices, not as rules
      ← "each node is named `meta-<quality> · virtue over/against vice` … `over/against`
         is a **lean, not a law** — the vice is sometimes legitimate and needs awareness."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/CLAUDE.md)
    a third module distils a generator rather than a word list
      ← "> **the reframe.** this is not a repo-tag whitelist (that stays deferred). it is
         a generative meta-language: `term = prefix(es) · ROOT · suffix` … distilling
         words chases infinity; distilling the generator is finite."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/vocabulary.md)

## how-it-is · technicality (external)

    two of the pipeline's governing rules are stated as wide-scope, reusable nodes
      ← "### capture-over-judge — gather wide, decide nothing while gathering / why
         judgment at fetch-time silently discards what a later pass would need"
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    and the refusal of verdicts is stated as one of them
      ← "### evidence-over-verdict — describe what IS, quote-grounded; refuse the verdict
         / why  the index orients; it never ranks or judges on the user's behalf"
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    the etymological module defers to the user's own sense of a root over the standard one
      ← "> **read it as a scaffold to CORRECT.** the meanings below are STANDARD Latin
         etymology. your idiolect may assign a root a different essential sense — yours
         wins."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/vocabulary.md)

## leaves — earned, not padded

    the documents instruct the reader to distrust them, in both files
      ← "> **audit before trusting.** a blueprint to BUILD FROM and to DISTRUST. …
         the map is not the territory."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    and say plainly that they are unfinished
      ← "> **audit before trusting.** this file is a blueprint to build from AND to
         distrust. it is not finished."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/CLAUDE.md)
    the spec records where a prior test run changed its own conclusion
      ← "[S4]          sharpened by the test run: position is not delivered BY the agent —
         it emerges IN the user, at a co-construction session; the index RECORDS the
         position taken."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/spec.md)
    the vocabulary module explicitly leaves its source file untouched
      ← "- the original file is unchanged. this module only references it."
         (https://raw.githubusercontent.com/anselmetbrl/skill-construction/6c1763a3/vocabulary.md)

#graph-harvest

    #void — no span in this repo names any other project, inside this collection or
      outside it. its references point exclusively at its own files and at internal
      source-refs. it is entirely self-referential in its citations.

## flags

    hype
      #void — none found. the repo's own documents instruct the reader to distrust them
      and state that they are unfinished; there is no promotional register anywhere
    filler
      #void — none found in the frozen material
    abandonment
      #void — the opposite of unreachable here: this is the one seed where the API
      answers, and it was pushed to the day it was fetched
    contribution
      #void — none found in the frozen material
    unfree?
      #void — the repository carries NO licence file and the API reports license null,
      so no terms are granted to anyone. an ABSENCE has no span to quote: recording it
      as a void with its reason is the only honest form. it is a real fact for the user
      to weigh, and it is not a paywall
    contradiction
      #void — none found in the frozen material

## finding — thin in every conventional signal, thick in prose

    164 KB of markdown across nine files, and simultaneously: no README, no licence, no
      description, no topics, no releases, no tags, no detected language, and two days
      old at fetch. both readings are true and neither is a verdict about the work
      #void — an observation about the source, carrying no claim about the project

## finding — the self-reference, handled by construction

    this seed is the home of the skill executing this run, and one of its branches is the
      branch this run commits to. pinning every span above to 6c1763a3 is what keeps the
      run from citing itself. whether the seed belongs in the INDEX remains open — see
      decisions.md D2
      #void — an observation about the run, carrying no claim about the project
