# bootstrap-build — paste this to open the BUILD session, in this repo
<!-- the message that starts construction of the skill itself. your voice, to the model. -->

## stance
we are CO-CONSTRUCTING, not producing. go at the speed of MY understanding, not yours.
`CLAUDE.md` auto-loads in this repo — it is your stance; follow it. above all:
    - one focus per message; no walls of text — i am partially blind with limited attention.
    - ask before acting; checkpoint-pause at each boundary; await my proceed.
    - mark guesses as guesses; surface collisions; never smooth the mess away.
    - do not settle for summaries — re-ingest the sources.
    - `spec.md` is a blueprint to BUILD FROM and to DISTRUST. audit it as you build.

## what we are building
the skill `holistic-meta-index`, from `spec.md`.
the spec is DONE — do NOT re-derive it, do NOT re-walk the deconstruction.
your job is to turn it into something that RUNS, and then to run it.

    build   the skill artifact — instructions an agent can actually execute
    run     on 3-5 repos i know deeply, so i can judge gist-quality myself
    fix     what the run breaks, under git, phase by phase

## read, in this order
    1. CLAUDE.md      (auto) — how to work with me
    2. spec.md        — the pipeline. claim-tree, each node folding into
                        moderation/modification falsification seeds
    3. ledger.md      — the layered log behind every decision in spec.md.
                        later supersedes earlier; corrections outrank guesses.
                        trace a source-ref before trusting the line that cites it.
    4. vocabulary.md  — my root-language. a generator, not a whitelist.
    raw, if you need the origin:
       indexing_autoinfo_hg_v0.1.md   the original mess. distrust it.
       complementarcontextualizinginformations_extractedfromchat.md   my own words, q&a

## the discipline — it is in the spec, honour it
    multi-shot, never one-shot. one phase per pass.
    git-checkpoint every phase — the environment is ephemeral, and a defect must be
    diffable and revertable rather than a whole-run rebuild.
    self-check (spec §2): micro after each pass, macro before and after each phase.
    the build is NOT the run. a perfect implementation that never crawled a real repo
    is worth nothing.

## resolve WITH me before building — do not assume
    packaging     is the deliverable an installable skill (`SKILL.md` with name +
                  description + instructions), or an instruction-file i paste?
                  the spec said "one file" for v1 — that may or may not still hold.
    artifact tree what lands where on disk: dossiers, gists, the index, the emerge
                  companion. the spec stays intent-level on purpose; name it with me.
    seeds         i give you 3-5 repos at run time. do not invent a seed list.

## answer these by FACT, not by guess — a small run settles all three
    auth        unauthenticated github api is ~60 req/hr. what do we actually need.
    context     the multi pass reads ALL gists together. where does that ceiling sit.
    scraping    do the retry-ladder rungs (plain → stealth → alternative → flag)
                actually work in this environment.

## still evolvable — they break in practice, not on paper
    significant-update heuristics (releases, non-bot commits, maintainer response) ·
    whether 2 relation-types hold at 300 repos · every moderation/modification seed

## first move
do NOT jump to producing. study `spec.md` and `ledger.md`, then reflect back — in a few
atomic lines — what you will build, and the first thing we must resolve together.
then we go, one focus at a time.
